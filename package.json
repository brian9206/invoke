{
  "name": "invoke",
  "description": "Invoke Serverless Management Platform",
  "author": "Brian Choi",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "shared",
    "invoke-cli",
    "invoke-admin",
    "invoke-execution",
    "invoke-gateway",
    "invoke-logger",
    "invoke-scheduler",
    "invoke-runtime/worker"
  ],
  "scripts": {
    "setup:env": "node scripts/setup-env.js",
    "start": "pm2 start ecosystem.config.js --attach --no-daemon",
    "stop": "pm2 stop ecosystem.config.js"
  },
  "dependencies": {
    "plain": "^0.0.0",
    "pm2": "^6.0.14"
  }
}
