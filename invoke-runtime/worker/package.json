{
  "name": "invoke-worker",
  "version": "3.0.0",
  "description": "Invoke worker shim",
  "scripts": {
    "build": "bun build src/index.ts --target bun --outfile dist/worker --minify --sourcemap --compile --bytecode"
  },
  "dependencies": {
    "js-yaml": "^4.1.1",
    "mime-types": "^3.0.2",
    "path-to-regexp": "^8.0.0",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/js-yaml": "^4.0.9",
    "@types/mime-types": "^2.1.4",
    "@types/node": "^20.19.39"
  }
}
