{
  "name": "invoke-runtime",
  "version": "3.0.0",
  "description": "Invoke Runtime - C++ supervisor + Bun worker shim",
  "scripts": {
    "build": "bun build src/worker-main.ts --target bun --outdir dist --bytecode",
    "build:supervisor": "cd .. && meson setup invoke-runtime/build invoke-runtime --backend=ninja && ninja -C invoke-runtime/build"
  },
  "dependencies": {
    "mime-types": "^3.0.2"
  },
  "devDependencies": {
    "@types/mime-types": "^2.1.4"
  }
}
