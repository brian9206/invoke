# Database Configuration
POSTGRES_DB=invoke_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=invoke_password_123

# MinIO Configuration
MINIO_ROOT_USER=invoke-minio
MINIO_ROOT_PASSWORD=invoke-minio-password-123
MINIO_ACCESS_KEY=invoke-minio
MINIO_SECRET_KEY=invoke-minio-password-123

# Application Secrets
JWT_SECRET=your_jwt_secret_change_in_production

# Application Configuration
MINIO_BUCKET=invoke-packages
NODE_ENV=production

# Database Connection
DB_HOST=postgres
DB_PORT=5432
DB_NAME=invoke_db
DB_USER=postgres
DB_PASSWORD=invoke_password_123

# MinIO Connection
MINIO_ENDPOINT=minio
MINIO_PORT=9000

# Service URLs
EXECUTION_SERVICE_URL=http://invoke-execution:3001

# Service Ports
ADMIN_PORT=3000
EXECUTION_PORT=3001
SCHEDULER_PORT=3002

# Execution Service Configuration
CACHE_DIR=/app/cache
RATE_LIMIT=100
EXECUTION_TIMEOUT=30000
CACHE_TTL_DAYS=7
MAX_CACHE_SIZE_GB=10

# Scheduler Configuration
SCHEDULER_INTERVAL=60000
TZ=UTC